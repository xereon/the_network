<?php
// ini_set('display_errors', 1);
// ini_set('display_startup_errors', 1);
// error_reporting(E_ALL);

// ob_start();
    //$http_referrer = getenv( "HTTP_REFERER" ); TODO
    //$connect = mysqli_connect("$db_host", "$db_user", "$db_pass", "$db_name") or die("Connection was not established");
        /*if ($connect) {
            echo "Connected";
        }        //code for testing database connection  */  
?>